<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctrls="clr-namespace:FlipView.Controls;assembly=FlipView"
             x:Class="FlipView.Page1">


  <StackLayout VerticalOptions="Center" HorizontalOptions="Center">

    <ctrls:Flip ItemsSource="{Binding Imgs}" HeightRequest="300" AutoPlay="False" Interval="2000">
      <ctrls:Flip.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition />
              <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Image Source="{Binding Key}" Grid.RowSpan="2" Aspect="AspectFill" />
            <StackLayout BackgroundColor="#333333" Opacity="0.5" Grid.Row="1" HeightRequest="30">
              <Label Text="{Binding Value}"  TextColor="White" VerticalOptions="CenterAndExpand" />
            </StackLayout>
          </Grid>
        </DataTemplate>
      </ctrls:Flip.ItemTemplate>
    </ctrls:Flip>
  </StackLayout>

</ContentPage>